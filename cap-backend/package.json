{
    "name": "cap-backend",
    "version": "1.0.0",
    "description": "CAP backend for duplicate check",
    "dependencies": {
        "@anthropic-ai/sdk": "^0.96.0",
        "@sap/cds": "^9.9.1",
        "dotenv": "^17.4.2"
    },
    "devDependencies": {
        "@cap-js/sqlite": "^2.4.0"
    },
    "cds": {
        "requires": {
            "db": {
                "kind": "sqlite",
                "credentials": {
                    "database": "db/local.db"
                }
            }
        }
    },
    "scripts": {
        "start": "cds watch"
    }
}
